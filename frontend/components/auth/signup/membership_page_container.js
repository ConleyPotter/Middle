import { connect } from "react-redux";
import MembershipPage from "./membership_page";

const msp = state => {
  return {};
};

const mdp = dispatch => {
  return {};
};

export default connect(
  msp,
  mdp
)(MembershipPage);
